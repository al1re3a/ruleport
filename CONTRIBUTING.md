# Contributing

Keep the core generator browser-compatible and dependency-free. New targets must include tests, a documented destination path, and an example showing the generated format. Run `npm run check` before submitting a pull request.
